import { Routes } from '@angular/router';
import { SensorBindingsComponent } from './sensor-bindings.component';

export default [
    {
        path: '',
        component: SensorBindingsComponent
    }
] as Routes;
